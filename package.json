{
  "name": "my-node-app",
  "version": "1.0.0",
  "description": "A simple Node.js application for Google App Engine",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "fetch:client": "rimraf ./my-angular-app && git clone --branch develop --single-branch https://github.com/jae-anonas/my-angular-app ./my-angular-app",
    "build:client": "cd ./my-angular-app && npm install --legacy-peer-deps && npm exec ng build --configuration production",
    "postinstall": "npm run fetch:client && npm run build:client"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^4.17.1",
    "mysql": "^2.18.1",
    "mysql2": "^3.14.1",
    "rimraf": "^6.0.1",
    "sequelize": "^6.37.7",
    "sequelize-cli": "^6.6.3"
  },
  "engines": {
    "node": "22.x"
  },
  "devDependencies": {
    "sequelize-auto": "^0.8.8"
  }
}
